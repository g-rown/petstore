spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/petstore_dev
    username: postgres
    password: admin
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

logging:
  level:
    com.petstore: DEBUG
    org.springframework.security: DEBUG
