spring:
  datasource:
    url: ${DATABASE_URL}
    hikari:
      maximum-pool-size: 5
  jpa:
    show-sql: false
  flyway:
    enabled: true

logging:
  level:
    com.petstore: INFO
    org.springframework.security: WARN
